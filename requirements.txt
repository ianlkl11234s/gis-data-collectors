# 核心依賴
requests>=2.28.0
schedule>=1.2.0
python-dotenv>=1.0.0

# HTTP API
flask>=3.0.0

# S3 儲存（選填）
boto3>=1.28.0

# Supabase 即時寫入（選填）
psycopg2-binary>=2.9.0

# FlightRadar24（非官方）
FlightRadarAPI>=1.3.0
beautifulsoup4>=4.12.0

# 衛星軌道計算（SGP4）
sgp4>=2.22

# WKT 幾何 parse（road_event collector 用）
shapely>=2.0.0

# YAML 設定檔（監控系統 config/ 資料夾用）
PyYAML>=6.0

# 新聞事件 collector（news_events：RSS parse + Gemini 地點抽取）
feedparser>=6.0.0
google-genai>=1.0.0

# 全球氣候 collectors（global_climate：GFS/CMEMS/CAMS 模式場解析）
numpy>=1.26.0            # 三支 collector 直接 import（先前僅靠 shapely 間接帶入）
xarray>=2024.1.0         # NetCDF / GRIB2 開檔（noaa_gfs.py / cmems.py / cams.py lazy import）
netCDF4>=1.6.0           # xarray 開 .nc 的 backend（CMEMS / CAMS）
cfgrib>=0.9.10           # xarray 開 GRIB2 的 engine（NOAA GFS）；需系統庫 eccodes（見 Dockerfile）
cdsapi>=0.7.2            # CAMS ADS API client
copernicusmarine>=2.0.0  # CMEMS subset CLI（cmems.py 以 subprocess 呼叫）
Pillow>=10.0.0          # climate_bake 烤 RGBA PNG texture

# 未來擴充
# pandas>=2.0.0
# aiohttp>=3.8.0
